<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.online.winterhills">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.update_time_out">
        </activity>
        <activity android:name=".activities.EnterDetailsActivity"/>
        <activity android:name=".activities.SelectFlatNoActivity" />
        <activity
            android:name=".activities.ActivityIN"
            android:label="@string/title_activity_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.Visitors_list_Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>